name: github-actions
run-name: ${{ github.actor }} runs codes
on: [push]
jobs:
  check-bats-version:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - uses: akhileshns/heroku-deploy@v3.12.13 # This is the action
        with:
          heroku_api_key: ${{secrets.32f9fa14-c2f0-43a5-ab60-615b195d2c49}}
          heroku_app_name: "API Score client" #Must be unique in Heroku
          heroku_email: "valevi@hotmail.fr"